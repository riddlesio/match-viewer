
const defaultPlayerSettings = {
    chrome: true,
    isPlaying: false,
    selector: '.GamePlayer',
    currentState: 0,
    descriptors: [],
    playbackTimeout: {
        min: 100,
        max: 1500,
    },
};

export default defaultPlayerSettings;
